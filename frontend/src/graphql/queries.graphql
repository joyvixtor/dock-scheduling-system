query ActiveTransferTasks {
  activeTransferTasks {
    id
    sku
    quantity
    inboundDockId
    outboundDockId
    status
    operatorId
    createdAt
  }
}

query ActiveInboundDocks {
  activeInboundDocks {
    id
    dockNumber
    status
  }
}
